/**
    This is the program that all "programmers" will use to implement features
    for the robots on a company
*/
public interface IRobot
{
    void startup();
    // void shutdown();
    // void materialIsReady();
}
